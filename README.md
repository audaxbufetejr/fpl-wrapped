# FPL League Wrapped

Static HTML summaries generated from league data. Open [`index.html`](index.html) locally, or use the GitHub Pages URL after the first deploy completes.

This repository is published automatically from the private [`fpl`](https://github.com/audaxbufetejr/fpl) pipeline via `scripts/publish_wrapped.py`.
